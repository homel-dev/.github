name: CI

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  standards:
    uses: ./.github/workflows/repository-ci.yml
    with:
      standards_ref: ${{ github.sha }}
      enforce_numbered_docs: true
